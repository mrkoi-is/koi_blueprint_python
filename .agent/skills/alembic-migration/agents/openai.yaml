interface:
  display_name: "Alembic Migration"
  short_description: "Generate and review Alembic migrations"
  default_prompt: "Use $alembic-migration to generate or review a database migration."

policy:
  allow_implicit_invocation: true
