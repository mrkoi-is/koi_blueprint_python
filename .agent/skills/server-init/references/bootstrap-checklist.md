# Bootstrap Checklist

- Read `docs/architecture.md`
- Confirm Python preferred version from version policy
- Initialize project with `uv`
- Copy `skeleton/` into project root
- Verify `.env.example`, `pyproject.toml`, `Dockerfile`, `Makefile`, and CI workflow are present
- Run Ruff, pyright, and pytest
- Confirm `/health` test passes
