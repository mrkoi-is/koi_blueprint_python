interface:
  display_name: "CI Setup"
  short_description: "Configure the standard CI pipeline"
  default_prompt: "Use $ci-setup to configure the standard CI pipeline."

policy:
  allow_implicit_invocation: true
